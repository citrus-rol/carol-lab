<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>字體 & 行距展示</title>
  <style>
    :root {
      --font-xl: clamp(1.25rem, 3vw, 3rem);
      --lh-xl: 1.2;

      --font-lg: clamp(1.1rem, 2.5vw, 2.25rem);
      --lh-lg: 1.25;

      --font-md: clamp(1rem, 2vw, 1.75rem);
      --lh-md: 1.4;

      --font-sm: clamp(0.875rem, 1.5vw, 1.25rem);
      --lh-sm: 1.5;

      --font-xs: clamp(0.75rem, 1.2vw, 1rem);
      --lh-xs: 1.55;

      --font-xxs: clamp(0.625rem, 1vw, 0.875rem);
      --lh-xxs: 1.6;
    }

    @media (max-width: 768px) {
      :root {
        --font-xl: clamp(1.5rem, 6vw, 2.25rem);
        --lh-xl: 1.2;

        --font-lg: clamp(1.25rem, 5vw, 1.75rem);
        --lh-lg: 1.3;

        --font-md: clamp(1rem, 4.5vw, 1.5rem);
        --lh-md: 1.4;

        --font-sm: clamp(0.9rem, 4vw, 1.25rem);
        --lh-sm: 1.5;

        --font-xs: clamp(0.8rem, 3.5vw, 1rem);
        --lh-xs: 1.55;

        --font-xxs: clamp(0.7rem, 3vw, 0.875rem);
        --lh-xxs: 1.6;
      }
    }

    body {
      font-family: system-ui, sans-serif;
      padding: 2rem;
      background: #f5f5f5;
      color: #243640;
    }

    .demo-block {
      margin-bottom: 2rem;
      padding: 1rem;
      border-radius: 12px;
      background: white;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .demo-label {
      font-size: 0.875rem;
      color: #666;
      margin-bottom: 0.5rem;
      font-family: monospace;
    }

    .xl { font-size: var(--font-xl); line-height: var(--lh-xl); }
    .lg { font-size: var(--font-lg); line-height: var(--lh-lg); }
    .md { font-size: var(--font-md); line-height: var(--lh-md); }
    .sm { font-size: var(--font-sm); line-height: var(--lh-sm); }
    .xs { font-size: var(--font-xs); line-height: var(--lh-xs); }
    .xxs { font-size: var(--font-xxs); line-height: var(--lh-xxs); }
  </style>
</head>
<body>
  <div class="demo-block">
    <div class="demo-label">--font-xl / --lh-xl</div>
    <div class="xl">這是一個標題字體（XL） - 行距 1.2</div>
  </div>

  <div class="demo-block">
    <div class="demo-label">--font-lg / --lh-lg</div>
    <div class="lg">這是一個大字體（LG） - 行距 1.25</div>
  </div>

  <div class="demo-block">
    <div class="demo-label">--font-md / --lh-md</div>
    <div class="md">這是一個中等字體（MD） - 行距 1.4</div>
  </div>

  <div class="demo-block">
    <div class="demo-label">--font-sm / --lh-sm</div>
    <div class="sm">這是一個小字體（SM） - 行距 1.5</div>
  </div>

  <div class="demo-block">
    <div class="demo-label">--font-xs / --lh-xs</div>
    <div class="xs">這是一個更小的字體（XS） - 行距 1.55</div>
  </div>

  <div class="demo-block">
    <div class="demo-label">--font-xxs / --lh-xxs</div>
    <div class="xxs">這是一個極小字體（XXS） - 行距 1.6</div>
  </div>
</body>
</html>
